# Pre-Prod End-to-End Verification — 2026-06-02

Branch: `polish/final-pass`. Stack already running (API :8080, web :5173, Postgres,
MinIO, Mailpit, Ollama). LLM provider: DeepInfra (reachable). Driver: Playwright MCP
(single browser, serial UI) + parallel Sonnet subagents for backend/test surfaces.

Session under test: logged in as **colnio** (`irogov@u.nus.edu`), workspaces **Dev Lab**
(active) + **Graphene Lab**. Per user: do not delete pre-existing data — only
create-then-delete throwaway entities.

## Result summary

| Phase | Surface | Status |
|---|---|---|
| 0 | Readiness + baseline tests | ✅ |
| 1 | Auth & shell | ✅ |
| 2 | Core data model UI | ✅ (1 fix, 1 open) |
| 3 | Page editor / BlockNote | ✅ (core) |
| 4 | Calendar / Meetings / Inbox / People | ✅ |
| 5 | Artifact workers + agent/backend surfaces | ⬜ |
| 6 | LLM chat + tool-calling | ⬜ |
| 7 | Risk-assessment workflows | ⬜ |
| 8 | Admin / settings / notifications | ⬜ |
| 9 | Synthesis & sign-off | ⬜ |

Legend: ✅ pass · 🟡 in progress · ⚠️ pass w/ notes · ❌ fail (fixed) · ⬜ not started

## Bugs found & fixed

| # | Severity | Surface | Issue | Status |
|---|---|---|---|---|
| **F3** | **HIGH (functional)** | Sample/Experiment/Iteration detail | **Entity dashboards never rendered** — identifier strip, status/method/params, linked samples, and the **React Flow lineage graph** live in an `entityDashboard` block, but `EntityPageEditor.makeSeedBlocks()` only seeds an empty paragraph, so **0 / 81 pages** contained the block. The headline lineage feature (C4) was unreachable. **Fixed** by rendering `EntityDashboard` directly in `EntityPageEditor` for the `description` slot of non-project entities (project excluded — `OverviewTab` renders its own). Works for all existing + new entities, no content mutation. Verified: sample lineage graph + experiment params/samples now render. tsc clean, vitest 128/128. | **fixed** `EntityPageEditor.tsx` |
| F2 | low (UX) | WorkspacesPage / sidebar | "New project" offered to PI override-viewers who aren't workspace members; clicking → backend 403 "you are not a member of this workspace". Button should be hidden/disabled for non-members. | open |

## Deferred / larger issues for user

_(none yet)_

## Test identities (from DB)
- `dev@graphene-lab.org` — global **admin**, **owner** of Dev Lab + Graphene Lab. Full CRUD.
- `irogov@u.nus.edu` (colnio) — global **pi**, approved, **no workspace membership** → can
  override-**read** any workspace but cannot mutate (explains the `admin.override_access`
  audit flood). The session started here.
- `e2e-pending@u.nus.edu` — pending signup created in Phase 1 (for Phase 8 approve/reject).
- `polish.test@u.nus.edu` — approved, no membership.

**Identity switch:** Phase 2+ require an owner to exercise create/edit/delete, so the run
switches to `dev@graphene-lab.org` (this also exercises the Phase-1 login flow). Working
mostly in the **Graphene Lab** seed workspace (CVD Graphene Growth + All-Carbon Resistive
Switches, with seeded samples/lineage/experiments).

---

## Phase 4 — Calendar / Meetings / Inbox / People ✅
- **Calendar**: FullCalendar month view; "+ New event" → New Event modal (project/title/kind/
  all-day/start/end/desc) → event renders on June 2; event detail modal (kind/project/time) →
  **Delete** removes it. Calendar/Timeline toggle, scope, month/week/list present. ✅
- **Meetings**: empty state → New Meeting modal (title/kind/start/agenda) → created; detail page
  renders (kind chip, datetime, editable Agenda + Notes, last-updated). ✅
  - Minor: detail page shows Agenda + Notes only; Decisions / Action-items sections not visible
    on an empty meeting (may be conditional). Low priority — note.
- **People**: role-grouped directory renders (Owners: Dev User). ✅
- **Inbox**: 50 items, All/System filter chips, grouped by Today. **All 50 are
  `admin.override_access` System notifications** — see finding F4 (audit/inbox noise). No
  approval/pi_flag items present, so Approve/Reject inbox flow is exercised in Phase 7/8. ✅

### Finding F4 (low) — override-access audit noise floods Activity + Inbox
A PI/admin viewing a workspace they aren't a member of generates an `admin.override_access`
audit event per access, which surface as both Home "Lab Activity" entries and **50 Inbox
System items**. Functionally correct (audit transparency) but noisy. Consider collapsing
repeated override events or excluding them from the user Inbox.

## Phase 3 — Page editor / BlockNote ✅ (core)
Editor exercised across every entity + project surface during Phases 2/4:
- BlockNote mounts, editable, autosave with "Saved" status badge, History/Save controls. ✅
- Block normalizer renders API/seed-authored pages (entity dashboards, prose). ✅
- Reference/attachment blocks (`@sample`/`@experiment`/`@artifact`, image/pdf/html embeds),
  ETag-conflict UI, and history diff/restore: covered by vitest `page-editor.test.ts` (green);
  deep interactive slash-menu/restore spot-check **deferred** to keep budget for LLM phases.
- Dev-only `flushSync` StrictMode warning persists (finding F1, benign).

## Phase 2 — Core data model UI ✅
Worked as **dev@graphene-lab.org** (owner). Created throwaway **E2E Sandbox** workspace +
**Graphene Anode E2E** project to keep seeded data intact.
- New Workspace dialog → workspace created. ✅
- **NewProjectWizard (4 steps)**: Basics → LLM Risk Assessment (skip) → Team → First
  Iteration. Project + first iteration created, opened. ✅
- Sample create (GR-ANODE-001, electrode) ✅; **JSONB property editor** add+save
  (`mass_loading_mg_cm2: 3.5`) ✅.
- Experiment create → **EX-1** human-readable code, in_progress ✅.
- Project **Edit details** dialog (rename) ✅; **Archive** (confirm dialog → redirect) ✅.
- Project Overview dashboard reflects counts + Recent Samples/Experiments ✅.
- **Sample lineage (React Flow)** + experiment dashboards — see bug **F3** (found + fixed):
  now render the full dashboard (lineage edges derived_from/split_from, linked experiments,
  params, status). Verified on seeded G-CVD-014 + EX-1.
- Login flow (deferred from Phase 1): wrong-password → "invalid email or password";
  correct → home. ✅ (note: a `.fill()`-after-error React controlled-input race caused one
  stale-submit 401; char-by-char typing works — likely a test-harness artifact, not app bug.)

## Phase 1 — Auth & shell ✅
- Shell renders; sidebar nav (Home/Workspaces/Inbox/Meetings/People/Calendar/Admin) present;
  workspace selector + projects tree expand/collapse work. ✅
- Command palette (⌘K): open, fuzzy filter, keyboard select+navigate. ✅
- Theme toggle: dark ⇄ light, light mode renders cleanly. ✅
- **Register flow**: `/register` renders; mismatch → "Passwords do not match"; valid submit →
  "Account request submitted, awaiting admin or PI approval". Created pending user
  `e2e-pending@u.nus.edu` for Phase 8 approve/reject. ✅
- Login/logout: **deferred to Phase 9** (only seed creds available, not colnio's password —
  avoid stranding the session). Auth-guard redirect to /login already exercised implicitly.
- Minor: `/register` is reachable while already authenticated (no redirect to home). Cosmetic.

## Phase 0 — Readiness & baseline

- Services: API `/openapi.json`, `/llms.txt`, web, Mailpit, MinIO console all → 200. ✅
- LLM provider: DeepInfra active in `aiconf.local.json`, reachable. ✅
- **vitest**: 17 files / 128 tests **all pass** (~2s). ✅
- **go test ./... -p 1**: `go build ./...` clean; 18 pkg ok, 6 failures — **all KNOWN-FLAKY**
  (internal/ai, internal/auth, internal/platform: "account awaiting approval" / scope-stub
  exhaustion / idempotency miss = documented test-DB-isolation flakes). No regressions. ✅
- Browser: authenticated session loads app shell cleanly. ✅
- Command palette (⌘K): opens, fuzzy search ("resistive" → All-carbon project), ↵ navigates. ✅

### Finding F1 (dev-only, won't-fix) — BlockNote `flushSync` warning
On every entity page (Overview tab etc.) React logs `flushSync was called from inside a
lifecycle method` from BlockNote `replaceBlocks` (attributed to `PageEditorCore.tsx`).
Block-load is correctly inside a `useEffect` (`PageEditorCore.tsx:472`); the warning is the
known BlockNote + React **StrictMode** dev artifact (`main.tsx:72`), stripped from prod
builds, and an `EditorBoundary` (`PageEditorCore.tsx:407`) guards real crashes. Page renders
fine. Classified benign — noted, not fixed (fix = cross-cutting BlockNote refactor, deferred).

### Observations (not yet triaged)
- Home "Lab Activity" feed is flooded with `admin.override_access` system events
  ("just now", repeating) — likely the dev-mode access-override middleware logging on
  every request. Audit noise; confirm it does not occur in prod auth path.
- Projects list shows several **duplicate** "Graphene-Enhanced Li-Ion Battery Electrode
  Development" entries (distinct IDs) — likely repeated seed/integration-script runs.
- Inbox badge shows 50 (cap?).
